# v1.17.2 release readiness

Prepared from v1.17.1 source `1a6152c45799dcb89bd73d23bf58c587d473f51e`
for [#195](https://github.com/hon454/github-pulls-show-reviewers/issues/195).
This patch updates development tooling and regression tests; extension runtime
behavior, permissions, accounts and listing copy are unchanged.

## Validation

- Vitest and `@vitest/coverage-v8` are pinned to 4.1.11. The migration required
  a precise test-helper mock type and cleanup of account-storage warning spies.
- Vitest 4 changed coverage accounting. The initial passing 1,216-test suite
  reported 93.60% function coverage, below the unchanged 95% gate. Eight added
  regression tests cover diagnostic cancellation/error cleanup, fallback reply
  validation, document-owned sign-in retirement, and background startup/options
  events. All 1,224 tests in 59 files pass.
- Lint and typecheck pass. Coverage is 93.64% statements, 88.15% branches,
  95.09% functions and 94.89% lines. No threshold or exclusion was weakened.
- The first `pnpm verify:release` browser stage failed because the sandbox
  blocked Chromium process launch (`EPERM`). Running `pnpm test:e2e` with
  browser execution permitted passed all 32 tests.
- Full dependency audit reports one moderate finding, down from three:
  `wxt > web-ext-run > firefox-profile > adm-zip@0.6.0`,
  GHSA-vwc7-r8mq-g2x9. The npm release is still 0.6.0 with no reported patched
  version. No finding is suppressed; #195 remains open for upstream remediation.
- `pnpm audit --prod --audit-level moderate` reports zero findings.
- All six `pnpm cws:assets` capture tests and `pnpm verify:cws` pass. The 15
  PNG hashes are identical to the individually reviewed v1.17.1 images; only
  package/lockfile source hashes changed in the capture manifest. There are no
  new visual differences or listing-copy changes to register.
- Production preflight and `pnpm zip:release` (including `pnpm zip:checked`
  and GitHub App configuration verification) pass after the TESTING capture.
  `pnpm verify:locales` verifies all five packaged catalogs.
- Credential-only [dry-run](https://github.com/hon454/github-pulls-show-reviewers/actions/runs/34300632100)
  succeeded at source/control `1a6152c45799dcb89bd73d23bf58c587d473f51e`.
  It performed no upload, submission or GitHub Release creation and did not
  expose current review status in its output.

Local checked ZIP: `github-pulls-show-reviewers-1.17.2-chrome.zip`, SHA-256
`1869ae8a2d71c7fc264109894540b3ebb1a2be2629d5c0bf3c8f06820a6f3c9f`.
Its member list and every file match the downloaded v1.17.1 GitHub Release
asset byte-for-byte except `manifest.json`, whose only change is version
`1.17.1` to `1.17.2`. This is local package evidence, not an Actions receipt.

## Release boundary

The maintainer confirmed during this preparation that v1.17.1 is still under
Chrome Web Store review. Do not cancel or replace that pending submission.
No v1.17.2 tag, CWS upload/submission or GitHub Release has been created.
After v1.17.1 publication, recheck fresh store state and origin/main, then use
the [canonical runbook](../chrome-web-store-agent-runbook.md) with the exact
reviewed v1.17.2 source. Existing release authorization is preserved; pending
review is an execution dependency, not a request for renewed blanket approval.

Prior dashboard access was rejected by the browser URL security policy. Do not
use alternate browser or indirect access to bypass it. User-provided current
state evidence is required where the supported dashboard remains unavailable.
