# v1.16.1 release readiness

Prepared on 2026-09-08 from integrated main
`9cb78fa17d920376ff50006eaa115549ea8b24d4`, containing the seven completed
fixes in [milestone 18](https://github.com/hon454/github-pulls-show-reviewers/milestone/18).
This preparation changes the version, release notes, current submission target
and generated capture provenance. The release source SHA is the final merged
preparation commit, to be recorded with the workflow receipts.

## Executed validation

- `bash scripts/run-with-github-app-env.sh pnpm verify:release`: lint,
  typecheck, 998 tests in 48 files, and all 24 packaged Chromium E2E tests passed
  without retries. Coverage: 95.77% statements/lines, 92.72% branches and
  97.92% functions. The E2E build uses TESTING identifiers; final production
  packaging was performed afterward.
- Two complete `pnpm cws:assets` runs passed all six capture tests each. The
  complete manifests were byte-identical across both runs. All 15 PNGs are
  byte-identical to the previously committed v1.16.0 images; only source hashes
  changed. Detailed listing descriptions and packaged name/summary are unchanged.
- `pnpm verify:cws` passed the five listing sources, all 15 image hashes,
  dimensions/format, links, order and current-source provenance checks.
- `pnpm zip:release` passed the production environment preflight, `zip:checked`
  and the final packaged GitHub App configuration check. No TESTING/dev App
  markers were accepted.
- Every ZIP member was compared byte-for-byte with `.output/chrome-mv3`,
  including the complete file inventory. `pnpm verify:locales` passed against
  this production output: exactly five complete catalogs and all manifest
  metadata references.
- The production package was loaded in a fresh, disposable, headed Chrome for
  Testing 147.0.7727.15 profile on macOS. Direct accessibility and screenshot
  inspection confirmed the Korean Auto-language options page renders its
  account, language and display controls, with the normal add-account button
  and maintainer App guidance. No blank page or missing-configuration warning.
  The profile contained no live accounts and external traffic was blocked.
- Fresh `pnpm audit --audit-level moderate` and
  `pnpm audit --prod --audit-level moderate` both reported zero vulnerabilities.

Local checked package: `github-pulls-show-reviewers-1.16.1-chrome.zip`,
249,197 bytes, SHA-256
`06e7d9e39baf27142bfc6c394f3ef5ed575bfba7d8308a36b0b93cbb39ba1033`.
Workflow packaging records its own hash; ZIP timestamps can change that hash.
Permissions remain `storage`, `alarms`, with only GitHub and GitHub API hosts.

## Individual store image review

Each of the 15 regenerated 1280×800 RGB images was opened and inspected.
The [capture manifest](../chrome-web-store-assets/capture-manifest.json) records
exact file names, source hashes, image hashes and rendering provenance.

| Locale | 01 before/after | 02 reviewer states | 03 options/diagnostics |
| --- | --- | --- | --- |
| en | PASS: clear captions, all rows visible | PASS: requested outlines, teams and badges fit | PASS: labels, controls and evidence fit |
| ko | PASS: 사용 전/후, no overlap | PASS: 리뷰어/팀 and badges readable | PASS: Korean text and diagnostic wrapping fit |
| ja | PASS: 導入前/後, no overlap | PASS: レビュアー/チーム and badges fit | PASS: Japanese labels and actions fit |
| zh_CN | PASS: 使用前/后, no overlap | PASS: 审阅者/团队 and badges readable | PASS: Simplified glyphs and controls fit |
| zh_TW | PASS: 使用前/後, no overlap | PASS: 審查者/團隊 and badges readable | PASS: Traditional glyphs and controls fit |

These synthetic TESTING captures establish visual readiness and reproducibility
on this Chromium/platform/font environment. They do not prove live account
access or saved dashboard image bytes. Since copy and all images are unchanged,
this release can use the ordinary tag-triggered release path after confirming
the existing listing is ready; no listing replacement is required.

## Release execution boundary

The [release note](v1.16.1.md), package version and current submission ZIP target
all use `1.16.1`. Validate the final preparation PR and main CI, fetch main and
tags, then use the [canonical runbook](../chrome-web-store-agent-runbook.md).
Create `v1.16.1` only at the reviewed main source. The normal push workflow runs
production preflight, release verification and checked packaging before upload
and submission for normal review and automatic publication after approval.

This document contains local preparation evidence, not CWS upload/submission
receipts or a claim of publication. Record the actual source SHA, run URL,
intent/result receipts, artifact identity, ZIP digest, dashboard state and
GitHub Release asset separately. Do not infer current CWS state from version
equality or reuse the earlier v1.16.0 receipt for the new package.

No live private-repository sign-in, signed automatic-update delivery or
screen-reader pronunciation was tested during this release preparation.
